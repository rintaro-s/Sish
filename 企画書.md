このプロジェクトは、Zshに変わる、新たなシェル環境を作成することが目的です。
実行エラーを日本語で出力して、入力補助やその他追加機能を実装します。
```
➜  Sish giu
zsh: command not found: giu
```
これを、
```
➜  Sish giu
Sish：お兄ちゃん！"giu"って何？"git"の間違いじゃない？tab二回でgitって入力するよ！
```
のように表示させたいです。
もちろん、色分けもきちんとして。

エラーに限らず、大幅に偏向を加えて極めてユーザーフレンドリーなターミナル環境を作成します。

ただし、Zshのコマンドはすべて使えるように後方互換性をもたせます。

シェルソフト単体ではできることが限られるため、新たたコンソールソフトであるSish-Consoleも作成します。

Sish-Consoleを使用することで、高度な入力補助やコードテンプレ、コードショートカット/コマンドリダイレクト(g→git、a-ins→sudo apt install等)、壁紙背景を設定・使用することができます。

また、補助LLMも導入することで様々なことができるようにします。その際はアドレスを設定してLMstudioを叩きます。もちろん、LLMを使わなくても完全に使えるように設計する。

---

## 1. Sish (zsh改造) で追加すべき機能と変更ポイント

zshのソースコード（`Src/`以下）を直接書き換え、キャラクター性（Sish）とGUI連携機能を埋め込みます。

### A. エラーハンドラの日本語・キャラ化

* **対象ファイル:** `Src/utils.c`, `Src/exec.c`
* **改造内容:** * `command_not_found` や各種 `zerr`（エラー出力関数）を検索し、出力をハードコードで日本語に書き換えます。
* 単なる文字列置換ではなく、エラーの種類（権限エラー、構文エラー、未定義コマンド）に応じて、Sishの口調が変わる分岐をC言語で直接実装します。


* **新機能:** エラー時に、あらかじめ用意した「アドバイスDB（またはローカルの小型LLM）」を叩き、解決策を提示するロジックの埋め込み。

### B. GUI連携用の「テレメトリ・パケット」送信機能

* **対象ファイル:** `Src/main.c`, `Src/jobs.c`
* **改造内容:**
* コマンド実行直前（`preexec`相当）と実行直後（`precmd`相当）に、**Unix Domain Socket** または **Named Pipe** を通じて、Sish-ConsoleへJSON形式のイベントを送信するコードを追加。
* これにより、GUI側は「今、どのコマンドが実行され、CPUをどれくらい使っているか」をリアルタイムに把握できます。



### C. 環境の「完全固定化」

* **対象ファイル:** `Src/init.c`
* **改造内容:**
* ユーザーが `.zshrc` を書き換えても無効化できない、Sishとしての「絶対的な設定」をソースレベルで固定します（プロンプトの初期値、エイリアス、キーバインドなど）。



---

## 2. Sish-Console (GUI) の機能定義

Sishを単なるターミナルで終わらせないための、GUI側の機能です。

| 機能名 | 内容 |
| --- | --- |
| **Character Layer** | 画面上にSish（キャラクター）を表示。シェルからのイベントを受け取り、喜怒哀楽を表現。 |
| **Command Visualizer** | パイプで繋がれたコマンドの流れを、ノードベースの図でリアルタイム表示。 |
| **Smart Snippets** | 過去の成功コマンドを「型」として保存。変数の部分だけGUIで入力して実行。 |
| **Global Search RAG** | 過去の全出力をベクトル検索可能に。GUI上で「あの時のエラー内容」を一瞬で検索。 |

---

## 3. 編集アプローチ：開発の手順

### STEP 1: エラーメッセージのハック

まず、`Src/utils.c` 内の `zwarn` や `zerr` といった関数を見つけます。

```c
// イメージ：Src/utils.c 内を改造
if (error_type == NOT_FOUND) {
    fprintf(stderr, "Sish：お兄ちゃん！'%s'って何？\n", cmd_name);
}

```

ここを、単純な `fprintf` ではなく、Sish-Consoleと通信して「エラーメッセージをGUI側にポップアップさせる」処理に書き換えます。

### STEP 2: IPCブリッジの実装

Sish-Console（GUI）とSish（シェル）が会話するためのソケット通信を `Src/main.c` に埋め込みます。

* Sish起動時に、`/tmp/sish.sock` を作成。
* GUI側がこれに接続し、シェル内の情報を双方向にやり取りします。

### STEP 3: コンパイルとバイナリ名の変更

`Makefile` を書き換え、生成されるバイナリ名を `zsh` ではなく `sish` に変更してビルドします。
